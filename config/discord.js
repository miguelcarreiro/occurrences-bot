const channels = {
    OCCURRENCES_CHANNEL_ID: process.env.OCCURRENCES_CHANNEL_ID,
};

const occurrenceRole = process.env.OCCURRENCES_MENTION_ROLE_ID;

module.exports = {
    channels,
    occurrenceRole,
};
